<?xml version="1.0" encoding="utf-8"?>

<!-- Animation for current activity exit when another activity is opened on top -->

<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:shareInterpolator="false">

    <!-- Blank animation to keep background window intact when opening a new activity on top-->

    <!--
    <alpha
        android:duration="@integer/window_transition_exit_alpha_duration"
        android:fillAfter="true"
        android:fillBefore="true"
        android:fillEnabled="true"
        android:fromAlpha="1.0"
        android:interpolator="@android:interpolator/fast_out_slow_in"
        android:toAlpha="1.0" />


    <scale
        android:duration="@integer/window_exit_duration"
        android:interpolator="@android:interpolator/fast_out_slow_in"
        android:fillAfter="true"
        android:fillBefore="true"
        android:fillEnabled="true"
        android:fromXScale="1.0"
        android:fromYScale="1.0"
        android:pivotX="50.0%"
        android:pivotY="50.0%"
        android:toXScale="1.05"
        android:toYScale="1.05" />
        -->
</set>